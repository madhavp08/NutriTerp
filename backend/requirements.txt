# Web framework and server
fastapi==0.115.*
uvicorn[standard]==0.30.*

# Database access
sqlalchemy==2.0.*
psycopg[binary]==3.2.*

# Settings loaded from environment / .env
pydantic-settings==2.*
python-dotenv==1.*

# Scraper
httpx==0.27.*
beautifulsoup4==4.*
lxml==5.*

# Auth
bcrypt==4.*
email-validator==2.*  # lets pydantic validate EmailStr fields

# ML (macOS: xgboost needs `brew install libomp` for OpenMP)
pandas==3.*
scikit-learn==1.*
xgboost==3.*
joblib==1.*

# Tests
pytest==8.*
httpx==0.27.*  # also used by FastAPI TestClient
